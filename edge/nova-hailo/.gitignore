.venv/
__pycache__/
*.pyc
logs/
session_logs/
*.jsonl
models/*.hef
models/*.hef.hailort-zoo
models/.hailo_llm_fetch.env
models/dtln/
!models/piper/
hailort.log
*.wav
.python-version
!bench/corpus/*.jsonl

# secrets
.env
.env.local
runtime/google_oauth/
runtime/hmi_settings.json

# Native LLM backend: build on the Pi, never commit the ABI-specific .so
csrc/build/
nova_hailo/backends/hailo_llm_cpp*.so

# fetch_models.sh clones (NeMo-Speech.cpp, parakeet.cpp) + built C ABI libs
cloned/
models/nemo_speech/*.so
models/nemo_speech/*.so.*
models/parakeet/*.so
models/parakeet/*.so.*

# Qt HMI: official client is Python/PySide; skip CMake trees and theme dumps
hmi_qt/.venv/
hmi_qt/build/
hmi_qt/nova-animation/
hmi_qt/**/__pycache__/
hmi_qt/*.zip
