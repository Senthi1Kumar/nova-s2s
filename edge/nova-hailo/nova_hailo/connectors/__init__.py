"""User-addable MCP connectors: registry + minimal HTTP client.

Not wired into the assistant's tool schema yet. See registry.py's module
docstring for the latency constraint that gates that step.
"""
from __future__ import annotations
